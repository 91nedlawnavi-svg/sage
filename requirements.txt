# Sage — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
quart>=0.19.0

# Async HTTP client (llama.cpp API calls)
httpx>=0.27.0

# No other runtime dependencies.
# Embeddings and inference are handled by llama.cpp server processes.
# No numpy, no torch, no transformers, no vector DBs.
