# Copy to .env (gitignored). Leftmost model is tried first.
SAGE_CHAT_MODELS=provider/primary-model,provider/fallback-model
PORT=6969
# Google AI Studio key used only to create short-lived Gemini Live call tokens.
# GEMINI_API_KEY=
# Seconds of silence before metabolism runs (default 300)
# SAGE_METABOLISM_DELAY=300
